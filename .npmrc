omit-lockfile-registry-resolved=true
# Route npm tarballs through the configured registry, but preserve the pinned
# GitHub archive used by tree-sitter-kotlin instead of rewriting its hostname.
replace-registry-host=npmjs
